<?xml version="1.0" encoding="utf-8" ?>
<Shell
    x:Class="Laboratoire04.AppShell"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:Laboratoire04.Views">
    <TabBar>
        <ShellContent
            Title="Liste Contact"
            ContentTemplate="{DataTemplate views:ListContactView}"
            Icon="user.ico"
            Route="ListContactView"
            Style="{StaticResource Key='ShellCustom'}" />
        <ShellContent
            Title="Ajouter Contact"
            ContentTemplate="{DataTemplate views:AddContactView}"
            Icon="add_user.ico"
            Route="AddContactView"
            Style="{StaticResource Key='ShellCustom'}" />
    </TabBar>

</Shell>
